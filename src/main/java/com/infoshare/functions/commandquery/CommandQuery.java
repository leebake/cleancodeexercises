package com.infoshare.functions.commandquery;

public class CommandQuery {

    public boolean set(String attribute, String value) {

        if (set("username", "karol"))

        if (attributeExists("username")) {
            setAttribute("username", "karol")
        }
    }
}
