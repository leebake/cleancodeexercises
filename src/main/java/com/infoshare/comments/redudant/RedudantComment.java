package com.infoshare.comments.redudant;

public class RedudantComment {

    public void comment() {
        // Sprawdzenie czy klientowi przysługuje rabat
        if (order.value > 1500 && order.customer.vip == true && order.payment.card == GOLD) {

        }

        if (order.canBeDiscounted()) {
    }
}
